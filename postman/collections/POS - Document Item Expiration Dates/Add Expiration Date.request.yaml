$kind: http-request
name: Add Expiration Date
url: "{{baseUrl}}/api/DocumentItemExpirationDates/Add?companyId={{companyId}}"
method: POST
headers:
  Content-Type: application/json
queryParams:
  companyId: "{{companyId}}"
body:
  type: json
  content: |-
    {
      "documentItemId": 34,
      "expirationDate": "2026-12-31T00:00:00"
    }
order: 2000
