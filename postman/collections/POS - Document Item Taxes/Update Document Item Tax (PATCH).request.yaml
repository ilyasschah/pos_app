$kind: http-request
name: Update Document Item Tax (PATCH)
url: "{{baseUrl}}/api/DocumentItemTaxes/Update?companyId={{companyId}}"
method: PATCH
headers:
  Content-Type: application/json
queryParams:
  companyId: "{{companyId}}"
body:
  type: json
  content: |-
    {
      "documentItemId": 34,
      "taxId": 5,
      "amount": 15.75
    }
order: 4000
