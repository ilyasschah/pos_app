$kind: http-request
name: Update Barcode (PATCH)
url: "{{baseUrl}}/api/Barcodes/Update?companyId={{companyId}}"
method: PATCH
headers:
  Content-Type: application/json
queryParams:
  companyId: "{{companyId}}"
body:
  type: json
  content: |-
    {
      "id": 11,
      "value": "987654321098"
    }
order: 5000
